gameConfig = {
    virtualScreenHeight = 1080,

    -- Camera
    camera = {
        -- defined around 1080px
        minVirtualHeight = 1080,
        maxVirtualHeight = 4000
    },

    -- Debug options
    debug = {
        all = false,
        shapes = true
    }
}
